"""Minimal stub of the `solaar` package.

The vendored `logitech_receiver` imports a few things from `solaar` (the GUI app)
— translations and a settings persister. keyboard_concert doesn't need Solaar's app, so
this stub provides just those symbols, letting the library run without the
`solaar` package installed.
"""
